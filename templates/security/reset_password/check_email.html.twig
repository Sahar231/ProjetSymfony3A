{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<main>
	<section class="p-0 d-flex align-items-center position-relative overflow-hidden">
		<div class="container-fluid">
			<div class="row">
				<div class="col-12 col-lg-6 d-md-flex align-items-center justify-content-center bg-primary bg-opacity-10 vh-lg-100">
					<div class="p-3 p-lg-5 text-center">
						<h2 class="fw-bold">Check Your Email</h2>
						<img src="/assets/images/element/03.svg" class="mt-5" alt="">
					</div>
				</div>
				<div class="col-12 col-lg-6 d-flex justify-content-center align-items-center">
					<div class="row my-5">
						<div class="col-sm-10 col-xl-12 m-auto text-center">
							<span class="mb-0 fs-1">📧</span>
							<h1 class="fs-2">Email Sent!</h1>
							<p class="mb-4">
                                If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
                                This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                            </p>
                            <a href="{{ path('app_login') }}" class="btn btn-primary">Back to Login</a>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>
</main>
{% endblock %}
