<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>{{ chapitre.title }}</title>
    <style>
        body { font-family: Arial, Helvetica, sans-serif; font-size: 12px; color: #222; }
        h1 { font-size: 20px; margin-bottom: 10px; }
        .content { margin-top: 15px; }
    </style>
</head>
<body>
    <h1>{{ chapitre.title }}</h1>
    <div class="content">{{ chapitre.getContentAsHtml()|raw }}</div>
</body>
</html>
